diff --git a/Gemfile b/Gemfile
index 49fbcad..1975ae8 100644
--- a/Gemfile
+++ b/Gemfile
@@ -11,6 +11,7 @@ end
 gem "rails", "3.2.11"
 
 # Supported DBs
+gem "sqlite3", group: :sqlite
 gem "mysql2", group: :mysql
 gem "pg", group: :postgres
 
diff --git a/Gemfile.lock b/Gemfile.lock
index d8be14b..55366b5 100644
--- a/Gemfile.lock
+++ b/Gemfile.lock
@@ -432,6 +432,7 @@ GEM
       multi_json (~> 1.0)
       rack (~> 1.0)
       tilt (~> 1.1, != 1.3.0)
+    sqlite3 (1.3.6)
     stamp (0.3.0)
     temple (0.5.5)
     test_after_commit (0.0.1)
@@ -544,6 +545,7 @@ DEPENDENCIES
   six
   slim
   spinach-rails
+  sqlite3
   stamp
   test_after_commit
   therubyracer
